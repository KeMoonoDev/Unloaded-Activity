unimined.minecraft {
    fabric {
        loader fabric_loader_version
        accessWidener = file("src/main/resources/${rootProject.archives_name}.classtweaker")
    }

    defaultRemapJar = false
    runs.off = true
}