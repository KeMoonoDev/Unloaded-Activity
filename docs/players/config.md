# Config options

Config options can be found over at `config/unloadedactivity.json` in your
Minecraft/server directory. If you don't wanna have to navigate your files to
change the config options, they can also be changed in-game using
`/unloadedactivity config`. (You will need to be in single player or have OP
level 4)
