!!! note "Default value: **24**"

Controls how many chunks Unloaded Activity is allowed to simulate per tick.

This does not apply for grouped simulations. For that, you have to change
[`maxGroupTickSize`](../maxGroupTickSize).
